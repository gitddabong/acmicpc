import math

n = int(input())
list = []

def isPrime(num):
    for i in range(num):
        if num == 0 or num == 1:
            continue

        





def primeList(limit):
    for i in range(limit):
        if i == 0 or i == 1:
            continue

        if isPrime(i):
            list.append(i)
        else:
            continue

primeList(n)

for lists in list:
    print(lists)

    